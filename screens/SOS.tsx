import { Text, View } from 'react-native';

export default function SOSScreen() {
    return (
        <View className="flex-1 justify-center items-center bg-white">
            <Text className="text-lg font-bold text-red-600">🚨 SOS Page</Text>
        </View>
    );
}